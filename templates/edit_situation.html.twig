<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>edit_situation</title>
  <style>
    .one_situation_container {
      /* display: flex; */
      border: solid 1px gray;
      border-radius: 8px;
      padding: 8px;
      margin: 32px;
      background-color: rgb(194, 194, 194);
      /* justify-content: space-between; */
      /* align-items: center; */
    }
    .one_situation_container > .trigger_name {
      flex-grow: 1;
    }
    .one_situation_container > .trigger_edit_form {
      margin-left: 8px;
    }
    .situation_edit_button {
      background: none;
      border: none;
      cursor: pointer;
      font-size: 18px;
    }
    .new_situation_container {
      margin-left: auto;
      margin-right: auto;
      width: 128px;
    }
    .situation_show_form {
      width: 100%;
    }
    .situation_show_button {
      background: none;
      border: none;
      cursor: pointer;
      text-align: left;
      width: 100%;
      padding: 0;
      font: inherit;
    }
  </style>
</head>
<body>
<h1>状況の編集画面</h1>
  <button type="button" onclick="history.back()">戻る</button>
  <a href="show_trigger">ホームに戻る</a>
  {% for situation in situations %}
    <div class="one_situation_container">
      <form method="post" action="edit_situation_name_exec" class="situation_show_form">
        <input type="hidden" name="trigger_id" value="{{trigger_id}}">
        <input type="hidden" name="situation_id" value="{{situation.id}}">
        <input class="situation_show_button" type="text" name="update_situation_name_text" value="{{ situation.situation_name }}">
        <button type="submit" class="message_edit_button">更新</button>
      </form>
    </div>
  {% endfor %}
</body>
</html>